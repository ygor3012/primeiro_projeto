🔎 PARTE 1 — Esses primeiros blocos
document.getElementById("matricular").addEventListener("click", function() {
    this.innerText = "Matrícula realizada!";
});
O que acontece aqui?

document → representa a página inteira

getElementById("matricular") → pega o botão

addEventListener("click", ...) → diz:

Quando clicar, execute isso

this.innerText = ... → muda o texto do botão

Você fez isso 3 vezes para o mesmo botão:

document.getElementById("matricular").addEventListener...

Isso funciona, mas não é organizado.

Depois você fez a versão melhor 👇

🔎 PARTE 2 — Essa é a parte importante
const botao = document.getElementById("matricular");
const frase = document.getElementById("feita");

Aqui você criou variáveis.

O que isso significa?

Em vez de procurar o botão toda hora:

document.getElementById("matricular")

Você guarda ele em:

botao

Isso deixa o código:

Mais limpo

Mais rápido

Mais profissional

🔎 PARTE 3 — Controle de estado
let ativo = false;

Isso é MUITO importante.

Você criou uma variável que controla o estado do botão.

Pense assim:

ativo = false → botão normal
ativo = true  → botão ativado

Isso é lógica booleana (true/false).

🔎 PARTE 4 — A lógica do clique
botao.addEventListener("click", function() {

Significa:

Quando clicar no botão, execute a função.

A parte do IF
if (!ativo)

O ! significa:

"se NÃO estiver ativo"

Então:

Se ativo for false → entra aqui

Se ativo for true → vai para o else

Parte quando ativa
botao.innerText = "Matrícula realizada!";
botao.style.backgroundColor = "blue";
frase.style.display = "block";
ativo = true;

Isso:

Muda texto

Muda cor

Mostra frase

Marca que agora está ativo

Parte quando desativa
botao.innerText = "MATRICULAR-ME";
botao.style.backgroundColor = "";
frase.style.display = "none";
ativo = false;

Isso:

Volta texto original

Remove cor (volta para CSS)

Esconde frase

Marca como desativado

🔎 PARTE 5 — Botão "Saiba Mais"

Mesma lógica, mas só controla mostrar/esconder.

let mostrando = false;

Isso controla se o texto está visível.

🧠 O QUE VOCÊ APRENDEU SEM PERCEBER

Você usou:

✔ Variáveis (const e let)
✔ Booleanos (true / false)
✔ Condição (if / else)
✔ Evento (click)
✔ Manipulação de DOM
✔ Manipulação de estilo
✔ Manipulação de texto

Isso já é base forte de JavaScript.

🚀 Agora vou te mostrar algo importante

Seu código funciona, mas você tem repetição no começo:

document.getElementById("matricular")...

Essas três primeiras partes são desnecessárias agora.

Você pode apagar essas:

document.getElementById("matricular").addEventListener...
document.getElementById("matricular").addEventListener...
document.getElementById("matricular").addEventListener...

Porque você já fez a versão organizada com botao.

🎯 Conclusão

Você não está mais copiando.
Você já está:

Entendendo eventos

Entendendo estado

Entendendo lógica

Isso é o que separa quem só copia de quem aprende.

Se você quiser subir mais um nível agora, posso te explicar:

O que é DOM de verdade

O que é escopo

O que é função anônima

Ou transformar seu código em versão ainda mais profissional usando classList

Qual você quer entender agora? 😎🔥